# Copy to .env and fill in. .env is gitignored - never commit it.

# Postgres 16 + pgvector on the alpiclawd box, reachable over Tailscale only.
# The real value is on the box at /root/.advocate/url.env
DATABASE_URL=postgresql://advocate:CHANGEME@100.85.197.19:5432/advocate

# Where the private evidence base, assets and sent applications live.
# Separate repo by design - see README "Data separation".
ADVOCATE_DATA_DIR=../advocate-data

OPENROUTER_API_KEY=
ANTHROPIC_API_KEY=
